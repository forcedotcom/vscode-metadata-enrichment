{
  "name": "salesforce-metadata-enrichment",
  "displayName": "Salesforce Metadata Enrichment",
  "description": "Enrich Salesforce metadata components in your local DX project directly from Visual Studio Code.",
  "version": "0.0.2",
  "publisher": "salesforce",
  "license": "Apache-2.0",
  "repository": {
    "url": "https://github.com/forcedotcom/vscode-metadata-enrichment"
  },
  "packageManager": "yarn@1.22.22",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "main": "dist/index.js",
  "activationEvents": [
    "workspaceContains:sfdx-project.json"
  ],
  "contributes": {
    "commands": [
      {
        "command": "sf.metadata.enrich",
        "title": "%command.metadata.enrich.title%"
      },
      {
        "command": "sf.metadata.enrich.context",
        "title": "%command.metadata.enrich.title%"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "sf.metadata.enrich"
        },
        {
          "command": "sf.metadata.enrich.context",
          "when": "false"
        }
      ],
      "explorer/context": [
        {
          "command": "sf.metadata.enrich.context",
          "when": "sf:metadata_enrichment_project_opened && isFileSystemResource"
        }
      ]
    }
  },
  "dependencies": {
    "@salesforce/core": "^8.24.3",
    "@salesforce/metadata-enrichment": "^0.0.15",
    "@salesforce/source-deploy-retrieve": "^12.31.14",
    "concat-map": "^0.0.2",
    "minimatch": "^9.0.5"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "@types/jest": "^30.0.0",
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.90.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "@vscode/vsce": "^3.0.0",
    "esbuild": "^0.25.0",
    "eslint": "^8.0.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-import-resolver-typescript": "^4.4.4",
    "eslint-plugin-import": "^2.29.0",
    "glob": "^13.0.6",
    "husky": "^9.1.7",
    "jest": "^30.3.0",
    "prettier": "^3.8.1",
    "pretty-quick": "^4.2.2",
    "ts-jest": "^29.4.6",
    "typescript": "^5.5.0"
  },
  "scripts": {
    "build": "tsc --build",
    "compile": "yarn build",
    "compile:clean": "tsc --build --clean",
    "bundle:extension": "node esbuild.config.mjs",
    "vscode:prepublish": "yarn bundle:extension",
    "vscode:package": "yarn bundle:extension && npx vsce package --allow-package-all-secrets",
    "vscode:local-install": "npm run vscode:package && code --install-extension salesforce-metadata-enrichment-$(node -p \"require('./package.json').version\").vsix",
    "vscode:local-uninstall": "code --uninstall-extension salesforce.salesforce-metadata-enrichment",
    "vscode:local-reinstall": "rm -f salesforce-metadata-enrichment-$(node -p \"require('./package.json').version\").vsix && npm run vscode:package && code --uninstall-extension salesforce.salesforce-metadata-enrichment ; code --install-extension salesforce-metadata-enrichment-$(node -p \"require('./package.json').version\").vsix",
    "lint": "eslint . --ext .ts",
    "prettier": "prettier --write .",
    "prettier-check": "prettier --check .",
    "commitlint": "commitlint",
    "clean": "tsc --build --clean && rm -rf out dist node_modules *.vsix",
    "test": "jest",
    "prepare": "husky"
  }
}
